diff --git a/scripts/lifeplanner-review.py b/scripts/lifeplanner-review.py
index 3c359d5..95f83d3 100644
--- a/scripts/lifeplanner-review.py
+++ b/scripts/lifeplanner-review.py
@@ -244,7 +244,7 @@ with sync_playwright() as playwright:
     check('wish and vision completion remain independent of each other and native tasks',independent_completion,page)
 
     def keyboard_drag():
-        before=read_doc(page)['wishes'];handle=root(page).locator('[data-life-wish]').nth(2).locator('.lp-block-handle')
+        before=read_doc(page)['wishes'];handle=root(page).locator(f'[data-life-wish="{before[2]["id"]}"]').locator('.lp-block-handle')
         handle.focus();handle.press('Space');handle.press('Home');handle.press('Space')
         page.wait_for_function('([key,id])=>JSON.parse(localStorage.getItem(key)).wishes[0].id===id',arg=[KEY,before[2]['id']])
         handle.focus();handle.press('Space');handle.press('End');handle.press('Escape')
diff --git a/src/components/lifeplanner/VisionEditor.jsx b/src/components/lifeplanner/VisionEditor.jsx
index 3d94a27..238ba05 100644
--- a/src/components/lifeplanner/VisionEditor.jsx
+++ b/src/components/lifeplanner/VisionEditor.jsx
@@ -140,7 +140,6 @@ export default function VisionEditor({ wish, original, seed = '', store, onClose
       role="dialog" aria-modal={!projectStep} aria-label={t('lifeplanner.vision')} tabIndex={-1} inert={projectStep ? '' : undefined}>
       <div className="lp-vision-body" data-lp-scroll>
         <fieldset className="lp-note-fields" disabled={busy}>
-          <legend className="sr-only">{t('lifeplanner.vision')}</legend>
           <div className="lp-outcome-row lp-note-line">
             <span className="lp-note-gutter" aria-hidden="true" />
             <textarea ref={outcome} rows={1} id="lp-outcome" data-initial-focus className="lp-note-outcome" aria-label={t('lifeplanner.outcome')}
