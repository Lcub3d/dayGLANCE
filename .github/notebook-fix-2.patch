diff --git a/scripts/lifeplanner-review.py b/scripts/lifeplanner-review.py
index 95f83d3..ff05ede 100644
--- a/scripts/lifeplanner-review.py
+++ b/scripts/lifeplanner-review.py
@@ -42,7 +42,7 @@ def read_doc(page):
 def wait_count(page, collection, count):
     # Web Locks make commits asynchronous; wait for the durable document,
     # rather than assuming the click event is itself a completed save.
-    page.wait_for_function('([key,collection,count]) => JSON.parse(localStorage.getItem(key))[collection].length === count', arg=[KEY,collection,count])
+    page.wait_for_function('([key,collection,count]) => JSON.parse(localStorage.getItem(key))?.[collection]?.length === count', arg=[KEY,collection,count])
 
 
 def set_checkbox(locator, checked):
diff --git a/src/components/lifeplanner/LifePlanner.jsx b/src/components/lifeplanner/LifePlanner.jsx
index 99a543f..d27bda4 100644
--- a/src/components/lifeplanner/LifePlanner.jsx
+++ b/src/components/lifeplanner/LifePlanner.jsx
@@ -120,7 +120,7 @@ export default function LifePlanner() {
     else if (window.confirm(L('discard'))) setShowLifePlanner(false);
   };
   const drag = useNotebookDrag(root, {
-    t: L,
+    t,
     onMenu: (group, id) => setMenu(current => current?.id === id ? null : { group, id }),
     onMove: (group, id, targetId, after, expectedIds) => write(() => store.commit(current => {
       const collection = group === 'principles' ? 'principles' : 'wishes';
diff --git a/src/components/lifeplanner/VisionEditor.jsx b/src/components/lifeplanner/VisionEditor.jsx
index 238ba05..09c91dd 100644
--- a/src/components/lifeplanner/VisionEditor.jsx
+++ b/src/components/lifeplanner/VisionEditor.jsx
@@ -39,7 +39,7 @@ export default function VisionEditor({ wish, original, seed = '', store, onClose
     if (!dirty || window.confirm(t('lifeplanner.discard'))) onClose();
   };
   const drag = useNotebookDrag(root, {
-    t: (key, options) => t(`lifeplanner.${key}`, options),
+    t,
     onMove: (_group, id, targetId, after, expectedIds) => {
       try { field('steps', moveNotebookItem(draft.steps, id, targetId, after, expectedIds)); return true; }
       catch { setError(t('lifeplanner.errors.conflict')); return false; }
diff --git a/src/components/lifeplanner/useNotebookDrag.js b/src/components/lifeplanner/useNotebookDrag.js
index e9bec73..3bb099e 100644
--- a/src/components/lifeplanner/useNotebookDrag.js
+++ b/src/components/lifeplanner/useNotebookDrag.js
@@ -50,7 +50,7 @@ export default function useNotebookDrag(root, { onMove, onMenu, t }) {
     active.current = null; clean();
     if (!a?.targetId || a.targetId === a.id) return;
     const ok = await latest.current.onMove(a.group, a.id, a.targetId, a.after, a.ids);
-    setMessage(latest.current.t(ok === false ? 'moveFailed' : 'moved', { title: a.title }));
+    setMessage(latest.current.t(ok === false ? 'lifeplanner.moveFailed' : 'lifeplanner.moved', { title: a.title }));
     if (a.handle.isConnected) a.handle.focus({ preventScroll: true });
   }
   useEffect(() => {
@@ -59,7 +59,7 @@ export default function useNotebookDrag(root, { onMove, onMenu, t }) {
     const escape = event => {
       if (event.key !== 'Escape' || !active.current) return;
       event.preventDefault(); event.stopImmediatePropagation(); cancel();
-      setMessage(latest.current.t('moveCancelled'));
+      setMessage(latest.current.t('lifeplanner.moveCancelled'));
     };
     const blur = () => cancel();
     document.addEventListener('keydown', escape, true);
@@ -70,9 +70,9 @@ export default function useNotebookDrag(root, { onMove, onMenu, t }) {
   function handleProps(group, id, title, disabled = false) {
     return {
       type: 'button', disabled, className: 'lp-block-handle',
-      'aria-label': latest.current.t('moveBlock', { title }),
+      'aria-label': latest.current.t('lifeplanner.moveBlock', { title }),
       'aria-describedby': descriptionId,
-      title: latest.current.t('handleHint'),
+      title: latest.current.t('lifeplanner.handleHint'),
       onPointerDown(event) {
         if (disabled || event.button !== 0 || !event.isPrimary) return;
         if (active.current) cancel();
@@ -107,7 +107,7 @@ export default function useNotebookDrag(root, { onMove, onMenu, t }) {
           else {
             active.current = { group, id, title, handle: event.currentTarget, ids: rows(group).map(el => el.dataset.lpSort), keyboard: true, targetId: id, after: false };
             event.currentTarget.closest('[data-lp-sort]').dataset.lpDragging = 'true';
-            setMessage(latest.current.t('moveInstructions'));
+            setMessage(latest.current.t('lifeplanner.moveInstructions'));
           }
         } else if (a?.keyboard && ['ArrowUp', 'ArrowDown', 'Home', 'End'].includes(event.key)) {
           event.preventDefault(); event.stopPropagation();
@@ -115,7 +115,7 @@ export default function useNotebookDrag(root, { onMove, onMenu, t }) {
           const position = event.key === 'Home' ? 0 : event.key === 'End' ? list.length - 1 : Math.max(0, Math.min(list.length - 1, current + (event.key === 'ArrowUp' ? -1 : 1)));
           mark(list[position], position > list.findIndex(el => el.dataset.lpSort === id));
           list[position]?.scrollIntoView({ block: 'nearest' });
-          setMessage(latest.current.t('movePosition', { number: position + 1 }));
+          setMessage(latest.current.t('lifeplanner.movePosition', { number: position + 1 }));
         } else if (event.key === 'F10' && event.shiftKey) {
           event.preventDefault(); latest.current.onMenu?.(group, id);
         }
